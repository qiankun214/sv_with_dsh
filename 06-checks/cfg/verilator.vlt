// lint 配置（--lint-only 时由 tools/sv.py 以位置参数传入本文件）
//
// ⚠️ 本文件的 `//` 注释不要以工具名开头：verilator 会把 `// <工具名> <单词>`
//    当成遗留元注释解析（例如 `// Verilator lint ...` 里的 "lint" 会被当作
//    未知指令），报 `Unknown verilator comment` / BADVLTPRAGMA 并让整个文件
//    被当作 Verilog 源文件，导致 lint 静默失效（5.020 与 5.052 均如此）。
//
// `verilator_config` 起头的行是配置指令；文件后缀 .vlt 表示纯配置。
// 原则：默认开启全部告警；只关掉「与本仓约定冲突」或「由其它工具负责」的规则。
// 每关一条都要在这个文件里写明理由，否则评审会问。

`verilator_config

// TB / 仿真文件不受可综合性规则约束
lint_off -rule UNUSED      -file "*_tb.sv"
lint_off -rule UNDRIVEN    -file "*_tb.sv"
lint_off -rule DECLFILENAME -file "*_tb.sv"

// 注：风格类告警由 verible 负责，靠命令行 `-Wno-style` 关闭（不在这里写规则名，
//     避免 verilator 版本间规则名不一致导致控制文件解析失败）。

// 例外示例（用完请删除，长期豁免要写进 06-checks/waivers/，这里只放文件级约定）：
// lint_off -rule WIDTH -file "*/vendor/*" -match "Operator ASSIGNW expects*"
