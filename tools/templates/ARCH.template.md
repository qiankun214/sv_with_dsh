---
id: {{ID}}
title: {{TITLE}}
status: draft
owner: agent
reviewer:
date:
upstream: {{UPSTREAM}}
artifacts: []
---

# {{ID}} {{TITLE}}

<!-- 本文档只回答三件事：整体架构、功能分解为模块、模块间连接。
     不写 module 内部结构（内部功能块/FSM/数据通路/寄存器清单）——那是 ③ 详细设计。
     不重复抄写 REQ-* 的端口表——接口以 ① 为唯一权威。 -->

## 方案概述

<!-- 一句话方案 + 模块级框图。图件（图源+渲染）放与本文件同名的目录：
     结构框图用 Mermaid + ASCII 的 .md；时序类图用 WaveDrom 的 .json + .svg。 -->

## 整体架构

<!-- 系统由哪些 module 组成、数据流/控制流怎么走、时钟/复位如何贯穿。
     框图只画到 module 边界，不画 module 内部。 -->

## 功能点覆盖

| 功能点 | 本方案如何满足 | 备注 |
|---|---|---|
| REQ-xxx | | |

## 功能分解为模块

<!-- 判据：每个 module 的预计 RTL ≤ 500 行；超过必须继续拆，远低于则不为拆而拆。
     module 名即后续 03-design/<module>/ 的目录名，必须一致。
     写清「为何不继续拆」。 -->

| 模块 | 职责（做什么 / 不做什么） | 预计 RTL 规模 | 对应文件 |
|---|---|---|---|
| | | | |

## 模块间连接

<!-- 连接框图（谁连谁、什么信号）+ 模块间接口表；只有一个 module 时写「无模块间连接」并说明理由。
     对外接口不在本文件定义，引用 REQ-* 的硬件接口章节。 -->

| 源模块 | 目的模块 | 信号 | 方向 | 位宽 | 说明 |
|---|---|---|---|---|---|
| | | | | | |

## 接口与协议

<!-- 只写模块间接口与软硬件接口结论；端口表引用 REQ-*，不要重复抄写。 -->

## 时钟与复位

| 时钟域 | 频率 | 来源 | 用途 |
|---|---|---|---|
| | | | |

- 跨时钟域策略：
- 复位策略（同步/异步、低有效）：

## 资源与时序预算

| 指标 | 预算 | 依据 |
|---|---|---|
| 面积（sky130 单元数） | | |
| 频率 | | |
| 延迟/吞吐 | | |
| RTL 规模 | | 功能分解判据 |

<!-- 并写明「验证与综合口径」：⑤ 回归扫哪些参数档位、⑥ 综合按哪档出面积、验证深度（顶层黑盒/子模块白盒）。 -->

## 取舍与被否方案

| 方案 | 否决理由 |
|---|---|
| | |

## 变更历史

| 日期 | 变更 | 影响 |
|---|---|---|
| | 初稿 | |
